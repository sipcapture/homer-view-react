/*
 * MessagesTab Messages
 *
 * This contains all the text for the MessagesTab container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MessagesTab';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MessagesTab container!',
  },
});
